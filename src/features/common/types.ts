export interface WithId {
    id:number;
}
export interface FormTypes {
    name: string,
    gmail: string,
    location: number;
    calendars:number;
    description:string;
    attachments: string;
}